(ns dev)


;; pull in dep mid programmming!
; (boot.core/merge-env! :dependencies '[[com.rpl/specter "1.1.0"]])


; (use 'com.rpl.specter)

; (in-ns 'user)

; ;; not sure why this is useful
; (boot-tools-deps.core/deps :config-files "../deps.edn" :quick-merge true)
















